import React from 'react';

const Hero = () => (
  <section className="hero-container">
    <div className="hero">
      <h1 className="hero-title">Welcome</h1>
      <h2 className="hero-subtitle">to Marvel World.</h2>
    </div>
  </section>
);

export default Hero;
